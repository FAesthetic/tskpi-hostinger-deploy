'use strict';
const form=document.querySelector('#withdraw-form');
form.addEventListener('submit',async event=>{event.preventDefault();if(!form.reportValidity())return;const button=form.querySelector('button');button.disabled=true;const out=document.querySelector('#withdraw-status');try{const body=Object.fromEntries(new FormData(form));body.confirm=true;const res=await fetch('/api/withdraw',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});const d=await res.json();if(!res.ok)throw Error(d.error);out.textContent='Euer Widerruf ist eingegangen. Ladet eure Eingangsbestätigung hier herunter. Eine Bestätigung per E-Mail wird ebenfalls versandt.';const link=document.querySelector('#receipt-download');link.href=URL.createObjectURL(new Blob([d.receipt],{type:'text/plain;charset=utf-8'}));link.download='Herzblende-Widerruf-Eingangsbestätigung.txt';link.hidden=false;button.hidden=true;}catch(e){out.textContent=(e.message||'Der Widerruf konnte gerade nicht übermittelt werden.')+' Ihr könnt ihn auch per E-Mail an uhighcauseidope@gmail.com senden.';}finally{button.disabled=false;}});
